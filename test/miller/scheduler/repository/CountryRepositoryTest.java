package miller.scheduler.repository;

import miller.scheduler.domain.Country;

import java.time.Instant;
import java.util.List;
import java.util.Optional;

public class CountryRepositoryTest {

    public static void main(String[] args) {
//        CountryRepository repository;
//
//        Country entity = new Country();
//        entity.setCountry("USA");
//        entity.setCreateDate(Instant.now());
//        entity.setCreatedBy("test");
//        entity.setLastUpdatedBy("test");
//        entity.setLastUpdate(Instant.now());
//        entity = repository.insert(entity, "AAAAA");
//
//        Assert.isNotNull(entity.getId());
//
//        Country savedEntity = repository.findById(entity.getId())
//                .orElseThrow(RuntimeException::new);
//
//        savedEntity.setCountry("USSR");
//        repository.update(savedEntity, "USER");
//
//        Country updatedEntity = repository.findById(entity.getId())
//                .orElseThrow(RuntimeException::new);
//        Assert.isEqual(updatedEntity.getCountry(), savedEntity.getCountry());
//
//        List<Country> allEntities = repository.findAll();
//        if(allEntities.isEmpty()){
//            throw new RuntimeException();
//        }
//
//        repository.delete(entity.getId());
//
//        Optional<Country> optional = repository.findById(savedEntity.getId());
//
//        if(optional.isPresent()){
//            throw new RuntimeException();
//        }
    }
}
